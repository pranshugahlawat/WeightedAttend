# AI Disclosure — WeightedAttend

This project was built by the author with assistance from AI tools for:
- brainstorming architecture and database schema ideas,
- generating starter code snippets,
- refactoring, documentation drafting, and bug-fixing suggestions.

All final implementation decisions, code review, testing, and integration were performed by the author.
Any external APIs/libraries used are credited in the README.

The project does not send user data to any AI service at runtime. OCR runs in the browser using Tesseract.js.


- Move `import React from "react";` to the top of the file
- Or split `SubjectsTable` and `SubjectStats` into components in `src/components/`